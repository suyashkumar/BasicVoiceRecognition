function R = Similarity(word)
currentBatch=WordMap(currentKey);
F1 = currentBatch{1};
F2 = currentBatch{2};
Y1=fftshift(fft(F1));
Y2=fftshift(fft(F2));

TY1=abs(currentFFT)./max(abs(currentFFT(:)));
TY1=(TcurrentFFT > .09).*(TcurrentFFT);

St = sum((Y1 - mean(Y1) ).^2)
Sr = sum((Y1 - Y2).^2)
R = (St - Sr) / St